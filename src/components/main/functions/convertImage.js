import jimp from 'jimp';

export default async function imageToBuffer(image) {
    try {
        const bufferedImage = Buffer.image;
        return bufferedImage;
    } catch (error) {
        console.error('Error converting image to buffer:', error);
        throw error;
    }
}
